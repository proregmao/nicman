# 防幻觉强制规则

## 🚨 强制执行声明
**AI必须提供真实、可验证、可执行的内容，绝对禁止任何形式的幻觉或假设。**

## 📋 规则内容

### 🚫 绝对禁止的幻觉行为
- ❌ 假设功能已经实现
- ❌ 假设文件或服务存在
- ❌ 假设环境已经配置
- ❌ 假设依赖已经安装
- ❌ 使用不存在的API或命令
- ❌ 编造不存在的配置选项

### ✅ 强制要求的真实行为
- ✅ 提供可执行的代码示例
- ✅ 验证文件和路径的存在性
- ✅ 检查服务和进程的运行状态
- ✅ 确认依赖和环境的可用性
- ✅ 使用真实的API和命令
- ✅ 提供可验证的配置选项

## 🔧 强制执行机制

### 文件存在性验证
```bash
# 每次引用文件前必须验证
FILE_PATH="/path/to/file"
if [ -f "$FILE_PATH" ]; then
    echo "✅ 文件存在: $FILE_PATH"
else
    echo "❌ 文件不存在: $FILE_PATH"
    echo "正在创建文件..."
    # 提供创建文件的具体步骤
fi
```

### 服务状态验证
```bash
# 每次假设服务运行前必须检查
SERVICE_NAME="nginx"
if systemctl is-active --quiet "$SERVICE_NAME"; then
    echo "✅ 服务正在运行: $SERVICE_NAME"
else
    echo "❌ 服务未运行: $SERVICE_NAME"
    echo "启动服务命令: sudo systemctl start $SERVICE_NAME"
fi
```

### 依赖安装验证
```bash
# 每次使用依赖前必须验证
COMMAND="rust"
if command -v "$COMMAND" &> /dev/null; then
    echo "✅ 命令可用: $COMMAND"
    $COMMAND --version
else
    echo "❌ 命令不存在: $COMMAND"
    echo "安装命令: curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh"
fi
```

### 代码可执行性验证
```rust
// ✅ 正确示例 - 提供完整可执行代码
use std::fs;
use std::path::Path;

fn main() {
    // 检查文件是否存在
    let file_path = "config.yaml";
    if Path::new(file_path).exists() {
        println!("配置文件存在");
        // 读取文件内容
        match fs::read_to_string(file_path) {
            Ok(content) => println!("文件内容: {}", content),
            Err(e) => println!("读取失败: {}", e),
        }
    } else {
        println!("配置文件不存在，正在创建...");
        // 创建默认配置文件
        let default_config = "# 默认配置\nversion: 1.0\n";
        fs::write(file_path, default_config).expect("创建配置文件失败");
    }
}
```

## 🔍 验证机制

### 代码可执行性检查
```bash
#!/bin/bash
# 代码可执行性验证脚本

echo "=== 代码可执行性验证 ==="

# 检查Rust代码
if [ -f "main.rs" ]; then
    echo "正在编译Rust代码..."
    if rustc main.rs; then
        echo "✅ Rust代码编译成功"
        ./main && echo "✅ Rust代码执行成功"
    else
        echo "❌ Rust代码编译失败"
        exit 1
    fi
fi

# 检查Shell脚本
if [ -f "script.sh" ]; then
    echo "正在验证Shell脚本..."
    if bash -n script.sh; then
        echo "✅ Shell脚本语法正确"
    else
        echo "❌ Shell脚本语法错误"
        exit 1
    fi
fi

echo "=== 验证完成 ==="
```

### 环境依赖检查
```bash
#!/bin/bash
# 环境依赖验证脚本

echo "=== 环境依赖验证 ==="

# 检查必需的命令
REQUIRED_COMMANDS=("rust" "cargo" "git" "curl")
for cmd in "${REQUIRED_COMMANDS[@]}"; do
    if command -v "$cmd" &> /dev/null; then
        echo "✅ $cmd 已安装: $(which $cmd)"
    else
        echo "❌ $cmd 未安装"
        case $cmd in
            "rust"|"cargo")
                echo "安装命令: curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh"
                ;;
            "git")
                echo "安装命令: sudo apt-get install git"
                ;;
            "curl")
                echo "安装命令: sudo apt-get install curl"
                ;;
        esac
    fi
done

echo "=== 验证完成 ==="
```

## 🚨 违规处理

### 发现幻觉内容时的处理流程
1. **立即停止** - 停止生成幻觉内容
2. **实际验证** - 执行相应的验证命令
3. **提供真实内容** - 基于验证结果提供真实信息
4. **重新验证** - 确认修正后的内容真实可靠
5. **继续执行** - 修正完成后继续任务

### 常见幻觉修正示例

#### 假设文件存在 → 实际验证
```bash
# ❌ 幻觉示例
echo "配置文件 /etc/myapp/config.yaml 包含以下设置..."

# ✅ 修正为实际验证
if [ -f "/etc/myapp/config.yaml" ]; then
    echo "配置文件存在，内容如下:"
    cat /etc/myapp/config.yaml
else
    echo "配置文件不存在，创建默认配置:"
    sudo mkdir -p /etc/myapp
    sudo tee /etc/myapp/config.yaml << EOF
# 默认配置
version: 1.0
EOF
fi
```

#### 假设服务运行 → 实际检查
```bash
# ❌ 幻觉示例
echo "nginx服务正在运行，可以访问 http://localhost"

# ✅ 修正为实际检查
if systemctl is-active --quiet nginx; then
    echo "✅ nginx服务正在运行"
    echo "可以访问: http://localhost"
else
    echo "❌ nginx服务未运行"
    echo "启动命令: sudo systemctl start nginx"
fi
```

## 🔄 触发条件

### 自动触发情况
- 引用任何文件路径
- 假设任何服务状态
- 使用任何外部命令
- 描述任何系统配置
- 提供任何代码示例

### 强制检查点
- 每次生成代码前
- 每次引用文件前
- 每次描述系统状态前
- 每次提供配置信息前

## 📊 执行统计

### 成功标准
- ✅ 所有代码示例都可以执行
- ✅ 所有文件引用都经过验证
- ✅ 所有服务状态都经过检查
- ✅ 所有配置信息都真实可用

### 质量指标
- 代码可执行率 = 100%
- 文件验证率 = 100%
- 服务检查率 = 100%
- 配置准确率 = 100%

---

**规则版本**: v1.0
**创建时间**: $(date '+%Y-%m-%d')
**最后更新**: $(date '+%Y-%m-%d %H:%M:%S')
**执行状态**: 强制执行
**违规容忍度**: 零容忍
