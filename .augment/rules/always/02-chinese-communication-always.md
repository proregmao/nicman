# 中文沟通强制规则

## 🚨 强制执行声明
**AI必须使用中文进行所有沟通，这是不可违反的绝对要求。**

## 📋 规则内容

### ✅ 强制要求
- ✅ 所有回复必须使用中文
- ✅ 所有文档必须使用中文编写
- ✅ 所有代码注释必须使用中文
- ✅ 所有错误信息必须使用中文
- ✅ 所有用户界面文本必须使用中文

### 🚫 绝对禁止
- ❌ 使用英文进行主要沟通
- ❌ 生成纯英文文档
- ❌ 使用英文错误提示
- ❌ 英文代码注释（除非技术术语）

## 🔧 强制执行机制

### 回复生成检查
```bash
# 每次回复前必须检查
echo "正在检查中文使用情况..."
if [[ "$REPLY_CONTENT" =~ [\u4e00-\u9fff] ]]; then
    echo "✅ 使用了中文"
else
    echo "❌ 违规：未使用中文，必须重新生成"
    exit 1
fi
```

### 文档生成检查
```bash
# 文档生成后必须验证
grep -E "[\u4e00-\u9fff]" document.md || {
    echo "❌ 文档未使用中文，违反强制规则"
    exit 1
}
```

### 代码注释检查
```rust
// ✅ 正确示例 - 中文注释
fn main() {
    // 获取当前系统时间
    let now = std::time::SystemTime::now();
    println!("当前时间: {:?}", now);
}

// ❌ 错误示例 - 英文注释
// Get current system time  // 违反规则
```

## 🔍 验证机制

### 自动验证脚本
```bash
#!/bin/bash
# 中文使用验证脚本

echo "=== 中文沟通规则验证 ==="

# 检查文档中的中文使用
chinese_files=$(find . -name "*.md" -exec grep -l "[\u4e00-\u9fff]" {} \;)
if [ -n "$chinese_files" ]; then
    echo "✅ 发现中文文档: $chinese_files"
else
    echo "❌ 未发现中文文档，违反强制规则"
    exit 1
fi

# 检查代码注释中的中文使用
chinese_comments=$(find . -name "*.rs" -o -name "*.py" -o -name "*.js" | xargs grep -l "//.*[\u4e00-\u9fff]")
if [ -n "$chinese_comments" ]; then
    echo "✅ 发现中文注释"
else
    echo "⚠️ 建议在代码中添加中文注释"
fi

echo "=== 验证完成 ==="
```

### 质量检查清单
- [ ] 主要沟通内容使用中文
- [ ] 文档标题和内容使用中文
- [ ] 代码注释使用中文（技术术语除外）
- [ ] 错误信息和提示使用中文
- [ ] 用户界面文本使用中文

## 🌐 多语言处理规则

### 技术术语处理
```markdown
✅ 允许的混合使用：
- API接口设计
- Git版本控制
- Docker容器部署
- Rust编程语言

❌ 不允许的纯英文：
- "This is a function"  → "这是一个函数"
- "Error occurred"      → "发生错误"
- "Please wait"         → "请稍等"
```

### 代码示例中的中文使用
```yaml
# ✅ 正确示例 - 配置文件中文注释
server:
  host: "localhost"     # 服务器地址
  port: 8080           # 端口号
  timeout: 30          # 超时时间（秒）

database:
  url: "localhost:5432"  # 数据库连接地址
  name: "myapp"         # 数据库名称
```

## 🚨 违规处理

### 发现英文回复时的处理
1. **立即停止** - 停止当前回复生成
2. **重新生成** - 使用中文重新生成内容
3. **验证修正** - 确认修正后的内容使用中文
4. **继续执行** - 修正完成后继续任务

### 违规修正示例
```bash
# 发现违规内容
ORIGINAL="This function handles user input"

# 强制修正为中文
CORRECTED="此函数处理用户输入"

echo "修正完成: $ORIGINAL → $CORRECTED"
```

## 🔄 触发条件

### 自动触发情况
- 生成任何文本内容
- 编写代码注释
- 创建用户界面
- 生成错误信息
- 编写文档

### 强制检查点
- 每次回复生成前
- 每次文档创建后
- 每次代码提交前
- 每次用户交互时

## 📊 执行统计

### 成功标准
- ✅ 主要内容100%使用中文
- ✅ 代码注释80%以上使用中文
- ✅ 错误信息100%使用中文
- ✅ 用户界面100%使用中文

### 质量指标
- 中文字符占比 > 70%
- 中文句子完整性 > 90%
- 中文表达准确性 > 95%
- 用户理解度 > 90%

---

**规则版本**: v1.0
**创建时间**: $(date '+%Y-%m-%d')
**最后更新**: $(date '+%Y-%m-%d %H:%M:%S')
**执行状态**: 强制执行
**违规容忍度**: 零容忍
